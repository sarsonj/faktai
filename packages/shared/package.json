{
  "name": "@tappyfaktur/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'No tests for shared yet'",
    "lint": "echo 'No lint for shared yet'",
    "format": "echo 'No format for shared yet'"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
