NODE_ENV=production
API_PORT=4000
TRUST_PROXY=1

# URL web aplikace (CORS + reset linky)
WEB_ORIGIN=https://app.example.cz
APP_BASE_URL=https://app.example.cz

# Postgres z CapRoveru (One-Click app)
DATABASE_URL=postgresql://tappy:supersecret@srv-captain--postgres:5432/tappyfaktur?schema=public

TZ=Europe/Prague
SESSION_SECRET=replace-with-long-random-secret
SESSION_TTL_HOURS=24
RESET_TOKEN_TTL_MINUTES=30

# Volitelné SMTP (pokud chybí, reset link je jen v logu API)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@fakturai.app

# Registry lookup timeouts
REGISTRY_TIMEOUT_MS=6000
REGISTRY_USER_AGENT=FakturAI/1.0 (+https://app.example.cz)

# XML verze
XML_SCHEMA_DPH_VERSION=current
XML_SCHEMA_KH_VERSION=current
